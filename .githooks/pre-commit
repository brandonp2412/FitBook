#!/usr/bin/env bash

set -euo pipefail

repo_root=$(git rev-parse --show-toplevel)
cd "$repo_root"

echo "== pre-commit: verifying pubspec.lock with the Flutter submodule =="
flutter/bin/flutter pub get --enforce-lockfile
